{
  "name": "tiny-toolkit",
  "version": "0.0.3",
  "description": "A collection of useful javascript game dev tools",
  "main": "./dist/index.js",
  "license": "MIT",
  "private": false,
  "scripts": {
    "release": "yarn build && np",
    "test": "ava",
    "build": "rm -rf dist && parcel build ./index.js --no-cache --no-source-maps --global tiny-toolkit"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.6.2",
    "@babel/register": "^7.6.2",
    "ava": "^2.4.0",
    "eslint": "^6.4.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "np": "^5.1.0",
    "parcel-bundler": "^1.12.3"
  },
  "ava": {
    "require": [
      "@babel/register"
    ]
  }
}
